System.ondata(function(data) {
	var h = [];
	for(var i in data) {
		for(var j in data[i]) {
			if(Array.isArray(data[i][j])) {
				for(var k in data[i][j]) {
					h.concat(data[i][j]);	
				}
			}
		}
	}
	Stat.histogram(h,2000,2400,300);		
});
